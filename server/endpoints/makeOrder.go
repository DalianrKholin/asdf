package server

import "net/http"

func (s *ApiDbEndpoints) MakeOrder(w http.ResponseWriter, r *http.Request) {
	/*connectOrder := s.DB.Database(DataBaseName).Collection(OrderCollection)
	connectProduct := s.DB.Database(DataBaseName).Collection(ProductsCollection)
	connectUser := s.DB.Database(DataBaseName).Collection(UsersCollection)
	*/

}
